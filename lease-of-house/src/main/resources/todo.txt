1、 发布房屋信息（进行中）

  1）查询地区信息，保存地区信息（未进行）
  
  2）保存房屋信息（已完成）
  
  3）保存上传的图片信息，保存图片信息的记录（已完成）
  
     -- 出现问题上传文件名存在中文或存在空格时浏览器请求会将中文或空格进行转码，转码后的文件名并不存在，导致浏览器无法加载出图片。
     -- 解决办法：保存图片时先对图片名称进行 URLEncode 加密。(还需要注意加密后文件名称长度问题)
     -- 仍存在问题，应用服务器内部字符编码格为 ISO-8859-1 ，在获取到字符串后需要进行 new String(str.getBytes("ISO-8859-1"),"UTF-8") 才可以
     
     -- 解决办法2：对文件名做 MD5 加密，得到 HASH 值后存储。（已解决）
     
  4）添加多上图片（未进行）
  
    -- 验证上传文件类型
    -- 验证上传文件大小
  
  5）返回成功消息（已完成）
  
  6）修改房屋信息（进行中）
  
    1）对已发布的文件的删除问题。（已完成）
      
      简单实现，建立一个当前时间戳的目录，每次在删除文件信息时把指定的文件删除。如果当前目录下没有文件了把当前目录也删除。
      
    2）缺少对 RegionInfoId 的处理（未进行）

  7）删除房屋信息
  
    1、删除房屋文件信息
      1）删除房屋文件信息对应的文件。
    2、删除房屋信息

2、 用户登录、注册、登出（进行中）

  1）用户登录（进行中）
  
    -- 待添加验证码，以及后续的登录验证操作
    -- 待做密码提交的 MD5 加密
  
  2）用户注册（进行中）
 
    -- 待添加验证码，以及密码的 MD5 加密存储
  
  3）用户登出（已完成）

2、 房屋信息列表（分页查询） -- 完成 （缺少查询条件）是否做多表分页查询？ 可以，查询两个表的数据

3、 导入地区信息数据（已完成）

    1）将第一级别的数据查询出来添加到数据库
    
    2）将第一级别的子集，查询出来添加到数据库，以及第一级别子集的下级 （循环递归）

    3）查询出来一个地区下所有的子级，子级的子级，从子级开始保存
    
        if level < 5 
        
          继续向下查询
          
          只做查询，将查询到的结果分层保存。保存一个个的 List，向上获取 ID 更新 List

完成 Servlet 版后，改为 Struts2 版
继续整合 MyBaits 、 Hibernate 
再使用 Spring          

1、 接口调用权限

2、 用户权限管理

3、 用户权限分层

4、 用户登录管理

